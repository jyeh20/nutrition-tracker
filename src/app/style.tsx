import { Theme } from "@mui/material/styles";

const useMainStyles = (theme: Theme) => ({
  root: {
    minHeight: "100vh",
  },

  userInfo: {
    marginTop: "15vh",
  },

  tracker: {
    marginTop: "3vh",
  },

  inputs: {
    marginTop: "3vh",
  },
});

export default useMainStyles;
